# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp

# OS
Thumbs.db
.DS_Store
desktop.ini

# Logs
*.log

# Claude Code local settings
.claude/

# Variables de entorno (secretos)
.env
.env.*
!.env.example

# Token del API v1 (secreto compartido con Rols One). NUNCA commitear, aunque
# alguien lo cree dentro de shared/data en local en vez de en ROLS_DATA_DIR.
erp_api_token.txt
shared/data/*.txt

# BD SQLite de runtime (la fuente de verdad tras la migración). En prod vive en
# ROLS_DATA_DIR (fuera del docroot); en local en shared/data/. NUNCA al repo.
*.db
*.db-wal
*.db-shm
*.db-journal

# Datos de runtime editados desde la UI. El repo lleva shared/data/*.json como
# SEED; en produccion la verdad vive en ROLS_DATA_DIR (fuera del docroot).
# Backups locales generados durante migraciones.
shared/data/.bak/
